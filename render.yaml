# Render blueprint: https://render.com/docs/blueprint-spec
# Point Render at this repo and it reads this file.
services:
  - type: web
    name: minicrawl-demo
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: MINICRAWL_PUBLIC
        value: "1"
